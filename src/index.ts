export type {
  Story,
  PageStory,
  StoryMeta,
  StoriesModule,
  MocksModule,
  Viewport,
  MockedContext,
  ResolvedStory,
  SvelteLookConfig,
} from './types.js'

export function define_config(config: import('./types.js').SvelteLookConfig): import('./types.js').SvelteLookConfig {
  return config
}
